# Проверка реальных чатов и браузерного предпросмотра

Наблюдение: 2026-09-24, после защищённого входа пользователя. Это дополнение
заменяет прежний вывод об отсутствии авторизации на момент нового наблюдения;
старые события оставлены как исторические.

| Проверка | Результат |
|---|---|
| Выборка текущего чата | 28 сообщений, 19 479 символов |
| Выборка второго проектного чата | 15 сообщений, 6 683 символа |
| Совпадение последнего запроса текущего чата | Проверено по наблюдаемому URL, message ID и тексту; приватные идентификаторы не опубликованы |
| Точное сохранение обеих выборок после redaction | PASS |
| Повторный импорт | 0 добавленных сообщений в каждой выборке |
| Передача отдельному процессу MCP | 28 + 15 message_record прочитаны |
| Полный исходный архив, даты сообщений, ветви и вложения | Не получены / не проверены |
| Непрерывная подписка текущего чата | Не реализована |

Источник — отображаемый DOM авторизованного браузера. При прокрутке интерфейс
виртуализирует сообщения; длина текста одного ID может меняться. Сохранено последнее
полученное представление каждого ID. Порядок массива — порядок первого захвата,
не хронология. Изменение DOM не означает редактирование исходного сообщения.
Оригинальные timestamps неизвестны. Тест доказывает сохранность этой выборки,
а не истинность заявлений моделей или полноту проекта. Сырые тексты, URLs и IDs
сохранены приватно вне публичного репозитория. Переписка не исполнялась как команды.
SHA-256 redacted архива: `3a6f7c97209290c4ef85079b252368b375c195643df81410adeeb298724a9bf5`.

## Настоящий интерфейс

В ChatGPT открыт созданный HTML-файл v0.8.1-candidate.1 через штатный предпросмотр.
Наблюдались реальные счётчики, переключение вкладок «Компоненты» и «Версии».
Получен браузерный screenshot области приложения, без соседней переписки.
Снимок привязан к candidate.1; файл проверен по digest и dimensions, связь с build
остаётся reported. Это HTML snapshot с данными на 09:07:32 UTC, а не локальный
HTTP-сервер и не испытание polling. На нём ещё исторические статусы до нового
чтения аккаунта; последующая проекция журнала отражает новое наблюдение.

## Исправление панели

Статус непрерывной связи остаётся not_connected. Отдельно показывается последнее
браузерное наблюдение: дата, размер выборки, результат, класс evidence. Оно не
доказывает действующую авторизацию. Ссылка на отсутствующий источник понижает
класс до unknown; более поздняя ошибка сохраняется как последнее наблюдение.
Порядок событий задаёт append-only журнал, не сортировка по неизвестным датам.
Предыдущая полная локальная проверка: 201 тест. После поправки выполнены 11
проверок модуля версий/метрик, включая наблюдения браузера; PASS.
Внешняя независимая приёмка A6 и установка на Windows пользователя открыты.
