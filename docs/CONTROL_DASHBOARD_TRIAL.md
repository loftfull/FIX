# Испытание контрольной панели и версий — 2026-09-24

Результат локального полного набора: **201 тест PASS**, 9.451 секунды.
Включены 10 новых проверок версий/метрик. JavaScript обоих inline scripts проходит
`node --check`; это не браузерная проверка поведения/внешнего вида.

Независимый агент воспроизвёл перезапись служебного имени и выполнение smudge-фильтра.
После исправлений он повторил 10 тестов успешно; его дополнительные замечания о
регистре Windows и ограниченном чтении существующего изображения также исправлены
до полного201-тестового прогона. Глобальная приёмка A6 этим не закрыта.

Реальный ранее полученный shared-chat архив:18 сообщений,94 726 символов.
Повторный импорт добавил0 сообщений; текст, метаданные, роли и исходные даты
сравнены полностью после redaction. Девять исходных временных инверсий сохранены.
Новый MCP-процесс прочитал18 сообщений. Панель показала1 импортированную сессию,
18 наблюдений сообщений,0 критериев, неизвестный общий процент и NOT_CONNECTED.
Это правильный отрицательный результат: архив не стал «живым подключением».

В доступном browser на chatgpt.com показаны «Войти» и предложение войти для
получения истории. Чаты аккаунта ещё не прочитаны. Требуется защищённый вход.
Предыдущий localhost browser trial был ENV_BLOCKED; реальный screenshot нового
интерфейса не получен. Нет сгенерированных картинок, выдаваемых за скриншоты.

Источники проверок текущей рабочей сессии:
- dashboard-version-trial/tests.log — полный вывод201 теста;
- dashboard-version-trial/real-chat-result.json — сравнительная квитанция;
- dashboard-version-trial/runner-receipt.json — реальное исполнение через терминал;
- tests/test_terminal_versions.py — воспроизводимые Git/image/metrics regressions.

Windows/Linux CI нового SHA проверяются отдельно от данного локального результата.
Checkpoint restore здесь возвращает код в новый worktree, не окружение, базы, LFS
или удалённую службу. Старый журнал не становится текущей памятью после возврата.
