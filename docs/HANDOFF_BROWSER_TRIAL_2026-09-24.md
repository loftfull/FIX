# Испытание исправленного переноса

База кода: 144d0cb (remote 41061b6bf44708b6ff01e993f909f6397819dc9a),
с добавленным в этом испытании fallback ручного копирования.
Пакет: terminal-view/v1, canonical fields; journal tip
788af76365dc3812995c612094a5af9bde3f4f9b6b85bd0bef26d1e4d073d05a,
275 записей. Это ограниченная зарегистрированная память, не полный архив аккаунта.

Реальный HTML preview открыт в авторизованном браузере. Clipboard API отклонён
окружением: автоматический success не показан. Исправление открывает readonly
полный текст вне перерисовываемого content, выделяет его, объясняет ручное
Ctrl/Cmd+C и позволяет закрыть блок с возвратом фокуса к кнопке.
Нажатие Ctrl/Cmd+C выполнено. Прочитан реальный browser clipboard; JSON после
вводного текста разобран и целиком сравнён с исходным пакетом: равенство true.
Все 7 ограничений, 2 chats, 3 conflicts и 2 search_queue сохранены.

Кнопка download нажата, обработчик подготовил скачивание. Сохранённые браузером
байты не получены для сравнения; end-to-end download остаётся NOT_VERIFIED.
Нельзя считать одну надпись об успехе доказательством сохранения файла.

Отдельный субагент без контекста получил только JSON-файл и задание прочитать
его как данные. Он восстановил назначение, следующий шаг, все 7 ограничений,
3 конфликта, 2 пункта поиска и двухэлементную цепочку chats. Указал правильный
journal tip, различил классы evidence, не объявил full acceptance или continuous
ingestion. Отсутствующее явно отменённое решение назвал неизвестным. Его ответы
сопоставлены координатором с полями пакета. Это проверка чтения пакета, не
независимая верификация первичных исторических утверждений и не внешний A6.

Ограничение найдено и сохранено: некоторые старые конфликты используют слово
«недоступны» без уточнения поздних частичных наблюдений. Полного экспорта по-прежнему
нет; формулировки текущих пробелов следует уточнять новым sourced patch, а не
стиранием исторических сообщений. Семантическое испытание отменённого решения
не состоялось: в выбранном пакете нет соответствующего эталона.

Пять dashboard tests PASS. Следующие проверки: реальные downloaded bytes,
передача примера явно superseded решения, актуализация coverage-конфликтов,
причинная проверка прежней потери хвоста. Автоматический переход при лимите,
Windows host и full A6 остаются открытыми.
