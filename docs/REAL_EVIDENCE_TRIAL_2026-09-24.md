# Испытание реальных источников, 23–24 сентября 2026

Статус: компоненты проходят испытания; весь агент остаётся candidate.

Полный локальный regression: **90 тестов PASS**, Python 3.12/Linux.
Наблюдатель отдельно проверен на реальном checkout FIX: первый запуск записал
одно наблюдение, второй — ноль. Дневной переход проверен с управляемым временем
в тестовом Git-проекте; многодневная эксплуатация на компьютере пользователя не заявляется.

## Корпус и результаты

| Источник | Объём | Проверка |
|---|---:|---|
| Публичный длинный чат Vibe Stack Discovery | 18 сообщений, 94 726 символов текста | Все поля сообщений после очистки совпали с исходными; повторный импорт не изменил журнал |
| Обсуждение INSTA Issue #32 | 139 комментариев, 234 096 символов текста | Все поля сохранились; повтор не добавил дубликаты |
| Два документа передачи INSTA | 2 документа | Импортированы как документы, не как исходные чаты |
| R54–R65 | 12 PR | Проверены метаданные и совпадение head предыдущего PR с base следующего |

Настоящий stdio MCP-клиент прочитал оба журнала: 18 и 141 событие соответственно.
Пагинация по 17 элементов не пропустила и не продублировала события; получение
одного события сохранило полный текст, исходный model ID и ссылки на источники.
Ни одно импортированное сообщение не получило статус verified.

В длинном чате metadata содержали `gpt-5-6-thinking`; отсутствующие ID остались
unknown. GitHub login не использовался как доказательство AI-авторства.

## Исправления по независимому аудиту

- Смешанные числовые, строковые и неизвестные даты больше не ломают Markdown.
- Изменённые метаданные сессии сохраняются отдельным наблюдением.
- Сохраняются raw_message, метаданные узлов и исходные поля разговора; ветви
  не склеиваются в выдуманную последовательную переписку.
- Отдельная новая AI-сессия правильно различила продуктовую R65 и ветвь
  визуального контроля, старую/новую стратегию, неизвестное авторство и runtime.
- Найденный при передаче пробел реестра PR/Issue/CI источников исправлен.

Аудит импортёра независимо повторил 28 релевантных тестов после исправлений.
Команды полного regression и платформенных проверок определены в CI.

Аудит наблюдателя нашёл выполнение Git clean filters и неочищенные секретоподобные
имена файлов. Фильтры отключены на время probes, пути хранятся значениями, а не
ключами. Независимый повтор обоих воспроизведений и 17 тестов observer прошёл.
Также transient-изменение файла не останавливает watch; оно пропускает один такт.
Не заявляется sandbox-защита от враждебной одновременной замены Git-конфигурации.

Источник-аудитор передачи INSTA обнаружил пропущенные QA authority/freeze/call-site
ограничения. Они добавлены в Markdown и critical_constraints JSON; приложены сырые
ответы jobs. Повторный аудит закрыл замечания к пакету, не к полноте всего проекта.

## Выводы по INSTA и их границы

Наблюдаемая цепочка R54–R65 сосредоточена на recovery. Новейший из двух переданных
документов явно заменяет старый приоритет: архитектурная граница состояния,
затем Image Viewer. Это документированное направление, не готовый функционал.
PR #47 закрыт без merge и заменён draft PR #97; это не доказательство тупиковости
всего направления. Пустые steps двух проверенных CI jobs не являются падением
тестов приложения. Процент готовности и виновная модель не назначались.

Полные INSTA-чаты не получены: поиск переписки завершился ошибкой. Их отсутствие
не подменялось уведомлениями или handoff-документами. Реальные каталоги компьютера
пользователя, полнота раннего R1–R53 и Android runtime не проверены.

Cold handoff и аудит доступных источников не закрывают полную приёмку A6.
Исходные пользовательские материалы испытания хранятся отдельно от публичного кода.

## Windows CI correction

Run 35959955707 executed 58 Windows tests: 1 failure and 6 errors were caused
by implicit system-codepage reads in four test modules. Explicit UTF-8 file I/O
replaces those defaults. Linux jobs passed; a fresh Windows run is required.
